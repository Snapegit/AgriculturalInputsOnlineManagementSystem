<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cl.dao.NongyezixunDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.cl.entity.NongyezixunEntity" id="nongyezixunMap">
        <result property="wenzhangbiaoti" column="wenzhangbiaoti"/>
        <result property="wenzhangjianjie" column="wenzhangjianjie"/>
        <result property="tupian" column="tupian"/>
        <result property="wenzhangneirong" column="wenzhangneirong"/>
        <result property="storeupnum" column="storeupnum"/>
    </resultMap>

    <select id="selectListView"
		resultType="com.cl.entity.view.NongyezixunView" >

		SELECT  nongyezixun.* FROM nongyezixun  nongyezixun 	        
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.cl.entity.view.NongyezixunView" >
		SELECT * FROM nongyezixun  nongyezixun <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	

</mapper>
